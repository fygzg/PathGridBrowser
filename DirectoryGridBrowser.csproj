<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net6.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ApplicationIcon>icon.ico</ApplicationIcon>
    <StartupObject>DirectoryGridBrowser.Program</StartupObject>
    <!-- 支持发布的 Windows 平台 -->
    <RuntimeIdentifiers>win-x64;win-x86;win-arm64</RuntimeIdentifiers>
  </PropertyGroup>

  <!-- 自包含发布：将 .NET 运行时打包进程序，目标机器无需安装 .NET -->
  <PropertyGroup>
    <SelfContained>true</SelfContained>
    <PublishSingleFile>true</PublishSingleFile>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
    <PublishReadyToRun>true</PublishReadyToRun>
  </PropertyGroup>

  <ItemGroup>
    <EmbeddedResource Include="icon.png" />
  </ItemGroup>

  <Target Name="GenerateAppIcon" BeforeTargets="PrepareForBuild" Inputs="icon.png" Outputs="icon.ico">
    <Exec Command="powershell -NoProfile -ExecutionPolicy Bypass -Command &quot;Add-Type -AssemblyName System.Drawing; $b=[System.Drawing.Bitmap]::FromFile('$(ProjectDir)icon.png'); $i=[System.Drawing.Icon]::FromHandle($b.GetHicon()); $f=[System.IO.File]::Create('$(ProjectDir)icon.ico'); $i.Save($f); $f.Close(); $b.Dispose()&quot;" />
  </Target>
</Project>
